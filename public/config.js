// Optional runtime API base URL. Loaded before the app so fetch() uses this instead of build-time default.
// For production: set to your backend URL, e.g. window.__API_BASE__ = 'https://api.yourdomain.com/api/v1';
// Leave empty to use NEXT_PUBLIC_API_URL from build.
window.__API_BASE__ = '';
